class NetSdkConst {
  // static const Duration initializeTimeout = Duration(seconds: 10);
  // static const Duration defaultRequesttTimeout = Duration(seconds: 60);
  // static const Duration defaultSendSocket = Duration(seconds: 5);
  // static const Duration defaultConnectionTimeout = Duration(seconds: 10);

  static const Duration defaultConfigRequestTimeout = Duration(seconds: 30);
  static const Duration defaultTorInitializationTimeout = Duration(minutes: 3);
}
